{
  "fmt": {
    "lineWidth": 100,
    "semiColons": true,
    "singleQuote": false
  },
  "lint": {
    "rules": {
      "exclude": ["no-import-prefix"]
    }
  },
  "tasks": {
    "check": "deno fmt --check . && deno lint . && deno test --allow-env .",
    "eval": "deno run --allow-env --allow-net=openrouter.ai eval/ranking-eval.ts"
  }
}
